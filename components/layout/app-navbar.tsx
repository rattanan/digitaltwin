"use client";

import { BellRing, Bot, Expand, Fullscreen, LogOut, Menu, Search, UserRound } from "lucide-react";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { NTLogo } from "@/components/branding/NTLogo";
import { Button } from "@/components/ui/button";

type ShellUser = { displayName: string; username: string; agency?: { nameTh: string } | null; roles: { code: string; nameTh: string }[] };

function LiveClock() {
  const [now, setNow] = useState(new Date());
  useEffect(() => { const timer = window.setInterval(() => setNow(new Date()), 1000); return () => window.clearInterval(timer); }, []);
  return <div className="hidden text-right sm:block"><p className="text-sm font-semibold text-white">{new Intl.DateTimeFormat("th-TH", { dateStyle: "medium", timeZone: "Asia/Bangkok" }).format(now)}</p><p className="font-mono text-xs text-[var(--nt-blue-light)]/80">{new Intl.DateTimeFormat("th-TH", { timeStyle: "medium", timeZone: "Asia/Bangkok" }).format(now)} น.</p></div>;
}

export function AppNavbar({ user, onOpenMenu }: { user: ShellUser; onOpenMenu: () => void }) {
  const router = useRouter();
  const [loading, setLoading] = useState(false);
  async function logout() { setLoading(true); await fetch("/api/v1/auth/logout", { method: "POST" }); router.replace("/login"); router.refresh(); }
  async function fullscreen() { if (!document.fullscreenElement) await document.documentElement.requestFullscreen(); else await document.exitFullscreen(); }
  return <header className="sticky top-0 z-30 flex h-[76px] items-center justify-between border-b border-white/10 bg-[rgba(8,11,18,.86)] px-4 backdrop-blur-xl sm:px-7"><div className="flex min-w-0 items-center gap-3"><Button variant="ghost" size="icon" className="lg:hidden" onClick={onOpenMenu} aria-label="เปิดเมนูด้านซ้าย"><Menu className="size-5" /></Button><div className="lg:hidden"><NTLogo compact className="size-8 rounded-lg" /></div><div className="min-w-0"><p className="hidden text-[10px] font-semibold uppercase tracking-[.2em] text-[var(--nt-yellow)]/80 sm:block">NT · Provincial Operations Center</p><h1 className="truncate text-base font-semibold text-white sm:text-lg">ศูนย์บัญชาการเมืองอัจฉริยะ</h1></div></div><div className="flex items-center gap-1.5 sm:gap-3"><button type="button" className="hidden size-9 items-center justify-center rounded-xl text-[var(--text-muted)] transition hover:bg-white/[.06] hover:text-white md:flex" aria-label="ค้นหาข้อมูล"><Search className="size-4" /></button><button type="button" className="relative flex size-9 items-center justify-center rounded-xl text-[var(--text-muted)] transition hover:bg-white/[.06] hover:text-white" aria-label="การแจ้งเตือน"><BellRing className="size-4" /><span className="absolute right-1.5 top-1.5 flex size-3.5 items-center justify-center rounded-full bg-[var(--nt-yellow)] text-[8px] font-bold text-[var(--background-primary)]">3</span></button><button type="button" className="hidden items-center gap-1.5 rounded-xl border border-violet-300/15 bg-violet-300/[.06] px-2.5 py-2 text-[10px] text-violet-200 transition hover:bg-violet-300/[.12] md:flex" aria-label="เปิด AI Assistant"><Bot className="size-3.5" />AI</button><div className="hidden h-8 w-px bg-white/10 sm:block" /><LiveClock /><div className="group relative flex items-center gap-2"><button type="button" className="flex size-9 items-center justify-center rounded-full bg-gradient-to-br from-[var(--nt-blue-light)]/35 to-[var(--nt-blue)]/10 text-white ring-1 ring-[var(--nt-blue-light)]/30" aria-label="เมนูผู้ใช้งาน"><UserRound className="size-4" /></button><div className="hidden text-right xl:block"><p className="text-xs font-medium text-white">{user.displayName}</p><p className="text-[10px] text-[var(--text-muted)]">{user.roles[0]?.nameTh ?? "ผู้ใช้งาน"}</p></div><div className="absolute right-0 top-11 hidden w-52 rounded-2xl border border-white/10 bg-[var(--background-card)] p-2 shadow-2xl group-focus-within:block group-hover:block"><div className="border-b border-white/10 px-3 py-2"><p className="text-xs text-white">{user.username}</p><p className="mt-1 text-[10px] text-[var(--text-muted)]">{user.agency?.nameTh}</p></div><button type="button" onClick={fullscreen} className="mt-1 flex w-full items-center gap-2 rounded-xl px-3 py-2 text-xs text-[var(--text-secondary)] hover:bg-white/[.06] hover:text-white"><Fullscreen className="size-3.5" />โหมดเต็มหน้าจอ</button><button type="button" onClick={logout} disabled={loading} className="flex w-full items-center gap-2 rounded-xl px-3 py-2 text-xs text-rose-200 hover:bg-rose-400/10"><LogOut className="size-3.5" />{loading ? "กำลังออกจากระบบ..." : "ออกจากระบบ"}</button></div></div><button type="button" onClick={fullscreen} className="hidden size-9 items-center justify-center rounded-xl text-[var(--text-muted)] transition hover:bg-white/[.06] hover:text-white sm:flex" aria-label="โหมดเต็มหน้าจอ"><Expand className="size-4" /></button></div></header>;
}
