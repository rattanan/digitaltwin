"use client";

import { BrainCircuit, Database, GitBranch, Info, LayoutDashboard, RadioTower, X } from "lucide-react";
import { useState } from "react";
import { architectureSteps } from "@/lib/landing-content";
import { SectionHeading } from "@/components/common/section-heading";

const icons = { RadioTower, GitBranch, Database, BrainCircuit, LayoutDashboard };

export function ArchitectureSection() {
  const [modalOpen, setModalOpen] = useState(false);
  return <section id="architecture" className="scroll-mt-24 bg-[var(--background-secondary)] px-5 py-24 sm:px-8 xl:px-12"><div className="mx-auto max-w-[1440px]"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><SectionHeading eyebrow="Architecture overview" title="โครงสร้างที่ขยายได้ตามเมือง" description="เชื่อมต่อข้อมูลจากต้นทาง ผ่านชั้นบูรณาการและแพลตฟอร์มข้อมูล ก่อนส่งต่อ insight ให้ศูนย์บัญชาการ" /><button type="button" onClick={() => setModalOpen(true)} className="inline-flex shrink-0 items-center gap-2 self-start rounded-xl border border-white/10 px-4 py-2.5 text-xs font-semibold text-[var(--text-secondary)] transition hover:border-[var(--nt-blue-light)]/40 hover:bg-white/[.05] hover:text-white sm:self-end"><Info className="size-4 text-[var(--nt-blue-light)]" />ดูรายละเอียดสถาปัตยกรรม</button></div><div className="relative mt-14 grid gap-3 lg:grid-cols-5">{architectureSteps.map((step, index) => { const Icon = icons[step.icon as keyof typeof icons]; return <div key={step.label} className="relative flex items-center gap-4 rounded-2xl border border-white/[.08] bg-white/[.025] p-5 lg:block lg:min-h-[180px] lg:p-6"><span className="flex size-11 shrink-0 items-center justify-center rounded-xl bg-[var(--nt-blue)]/15 text-[var(--nt-blue-light)]"><Icon className="size-5" /></span><div className="min-w-0 lg:mt-7"><p className="text-sm font-semibold text-white">{step.label}</p><p className="mt-2 text-xs leading-5 text-[var(--text-muted)]">{step.description}</p></div>{index < architectureSteps.length - 1 && <span className="absolute -bottom-4 left-1/2 z-10 hidden h-5 w-px bg-gradient-to-b from-[var(--nt-blue-light)] to-transparent lg:-right-5 lg:left-auto lg:top-1/2 lg:block lg:h-px lg:w-6 lg:bg-gradient-to-r" />}</div>; })}</div></div>{modalOpen && <div className="fixed inset-0 z-[70] flex items-center justify-center bg-black/70 p-5 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="architecture-modal-title"><div className="w-full max-w-2xl rounded-3xl border border-white/10 bg-[var(--background-card)] p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[.2em] text-[var(--nt-yellow)]">Platform architecture</p><h3 id="architecture-modal-title" className="mt-2 text-2xl font-semibold text-white">ภาพรวมการไหลของข้อมูล</h3></div><button type="button" onClick={() => setModalOpen(false)} className="rounded-xl p-2 text-[var(--text-muted)] hover:bg-white/[.06] hover:text-white" aria-label="ปิดรายละเอียดสถาปัตยกรรม"><X className="size-5" /></button></div><div className="mt-6 space-y-3">{architectureSteps.map((step, index) => <div key={step.label} className="flex items-center gap-3 rounded-xl border border-white/[.07] bg-white/[.025] px-4 py-3"><span className="font-mono text-xs text-[var(--nt-yellow)]">0{index + 1}</span><span className="text-sm font-medium text-white">{step.label}</span><span className="text-xs text-[var(--text-muted)]">{step.description}</span></div>)}</div><p className="mt-6 text-xs leading-6 text-[var(--text-muted)]">รายละเอียดเชิงเทคนิค เช่น data contract, retention policy, observability และสิทธิ์การเข้าถึง จะกำหนดร่วมกับหน่วยงานใน phase การออกแบบ solution architecture</p></div></div>}</section>;
}
