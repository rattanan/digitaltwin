import { ArrowUpRight, Check, ShieldCheck } from "lucide-react";
import { landingBenefits } from "@/lib/landing-content";
import { SectionHeading } from "@/components/common/section-heading";

export function BenefitsSection() {
  return <section id="benefits" className="scroll-mt-24 bg-[var(--background-secondary)] px-5 py-24 sm:px-8 xl:px-12"><div className="mx-auto grid max-w-[1440px] gap-14 lg:grid-cols-[.85fr_1.15fr] lg:items-center"><div><SectionHeading eyebrow="Key benefits" title="ทำให้การบริหารเมืองเห็นภาพเดียวกัน" description="Digital Twin ช่วยให้ข้อมูลที่เคยแยกกันอยู่ถูกนำมาใช้ร่วมกัน ทั้งในงานประจำวัน การรับมือเหตุการณ์ และการวางแผนระยะยาว" /><a href="#architecture" className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-[var(--nt-blue-light)] hover:text-white">ดูภาพรวมสถาปัตยกรรม <ArrowUpRight className="size-4" /></a></div><div className="grid gap-3 sm:grid-cols-2">{landingBenefits.map((benefit, index) => <div key={benefit} className="flex gap-3 rounded-2xl border border-white/[.07] bg-white/[.025] p-4 transition hover:border-[var(--nt-blue-light)]/30 hover:bg-white/[.05]"><span className="flex size-7 shrink-0 items-center justify-center rounded-lg bg-emerald-300/10 text-emerald-200"><Check className="size-4" /></span><div><p className="text-sm leading-6 text-[var(--text-secondary)]">{benefit}</p><span className="mt-2 block text-[10px] font-mono text-[var(--text-muted)]">BENEFIT_{String(index + 1).padStart(2, "0")}</span></div></div>)}</div><div className="lg:col-span-2 flex items-center gap-3 rounded-2xl border border-[var(--nt-yellow)]/20 bg-[var(--nt-yellow)]/[.05] px-5 py-4 text-sm text-[var(--text-secondary)]"><ShieldCheck className="size-5 shrink-0 text-[var(--nt-yellow)]" /><span>ออกแบบให้ขยายจากจังหวัดสิงห์บุรีไปยังจังหวัด เทศบาล และองค์กรปกครองส่วนท้องถิ่นอื่นได้ตามบริบทและสิทธิ์การเข้าถึงข้อมูล</span></div></div></section>;
}
